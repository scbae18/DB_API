# Shopping Mall API 명세서

## 📋 개요

쇼핑몰 데이터베이스 조회를 위한 RESTful API입니다. PostgreSQL 데이터베이스에 저장된 상품, 회원, 주문, 배송 정보를 조회할 수 있습니다.

**Base URL**: `http://YOUR_SERVER_IP:3000`

---

## 🔗 API 엔드포인트 목록

### 1. 상품 정보 조회
### 2. 회원 정보 조회
### 3. 회원 주문 및 송장번호 조회
### 4. 송장번호로 배송정보 조회

---

## 1️⃣ 상품 정보 조회

### 요청

```
GET /api/products/:productId
```

### 파라미터

| 파라미터 | 타입 | 위치 | 필수 | 설명 |
|---------|------|------|------|------|
| productId | string | path | ✅ | 상품ID (예: PRD-0001) |

### 요청 예시

```bash
curl http://YOUR_SERVER_IP:3000/api/products/PRD-0001
```

### 응답 예시

**성공 (200 OK)**

```json
{
  "success": true,
  "data": {
    "상품ID": "PRD-0001",
    "카테고리": "의류",
    "상품명": "슬림핏 청바지",
    "브랜드": "리바이스",
    "정가(원)": 173000,
    "판매가(원)": 173000,
    "할인율(%)": 0,
    "재고수량": 12,
    "상품상태": "판매중",
    "등록일": "2024-04-08",
    "최종수정일": "2024-04-25",
    "상품설명": "리바이스 슬림핏 청바지 - 고품질 의류 상품입니다.",
    "대표이미지URL": "https://shop.example.com/img/prd_0001.jpg",
    "평점": 4.5,
    "리뷰수": 692,
    "판매수": 4477
  }
}
```

**실패 (404 Not Found)**

```json
{
  "success": false,
  "message": "상품을 찾을 수 없습니다."
}
```

### 응답 필드 설명

| 필드 | 타입 | 설명 |
|------|------|------|
| success | boolean | 요청 성공 여부 |
| data | object | 상품 정보 객체 |
| data.상품ID | string | 상품 고유 ID |
| data.카테고리 | string | 상품 카테고리 |
| data.상품명 | string | 상품명 |
| data.브랜드 | string | 브랜드명 |
| data.정가(원) | number | 정가 |
| data.판매가(원) | number | 판매가 |
| data.할인율(%) | number | 할인율 |
| data.재고수량 | number | 재고 수량 |
| data.상품상태 | string | 상품 상태 (판매중, 품절 등) |
| data.등록일 | string | 등록일 (YYYY-MM-DD) |
| data.최종수정일 | string | 최종 수정일 (YYYY-MM-DD) |
| data.상품설명 | string | 상품 설명 |
| data.대표이미지URL | string | 대표 이미지 URL |
| data.평점 | number | 평점 (0-5) |
| data.리뷰수 | number | 리뷰 개수 |
| data.판매수 | number | 판매 수량 |

---

## 2️⃣ 회원 정보 조회

### 요청

```
GET /api/members/:memberId
```

### 파라미터

| 파라미터 | 타입 | 위치 | 필수 | 설명 |
|---------|------|------|------|------|
| memberId | string | path | ✅ | 회원ID (예: USR-0001) |

### 요청 예시

```bash
curl http://YOUR_SERVER_IP:3000/api/members/USR-0001
```

### 응답 예시

**성공 (200 OK)**

```json
{
  "success": true,
  "data": {
    "회원ID": "USR-0001",
    "로그인ID": "user001",
    "비밀번호(해시)": "$2b$12$c56t4ufel6246hid25owf7",
    "이름": "이서윤",
    "이메일": "user001@kakao.com",
    "전화번호": "010-5533-1722",
    "생년월일": "1975-06-25",
    "성별": "F",
    "우편번호": "27146",
    "기본주소": "경기도 성남시 분당구",
    "상세주소": "B동 805호",
    "가입일": "2023-11-09",
    "최근로그인": "2024-05-22",
    "회원등급": "GOLD",
    "적립포인트": 28028,
    "총주문수": 47,
    "총주문금액(원)": 3661864,
    "상태": "활성",
    "마케팅동의": "Y"
  }
}
```

**실패 (404 Not Found)**

```json
{
  "success": false,
  "message": "회원을 찾을 수 없습니다."
}
```

### 응답 필드 설명

| 필드 | 타입 | 설명 |
|------|------|------|
| success | boolean | 요청 성공 여부 |
| data | object | 회원 정보 객체 |
| data.회원ID | string | 회원 고유 ID |
| data.로그인ID | string | 로그인 ID |
| data.비밀번호(해시) | string | 해시된 비밀번호 |
| data.이름 | string | 회원 이름 |
| data.이메일 | string | 이메일 주소 |
| data.전화번호 | string | 전화번호 |
| data.생년월일 | string | 생년월일 (YYYY-MM-DD) |
| data.성별 | string | 성별 (M/F) |
| data.우편번호 | string | 우편번호 |
| data.기본주소 | string | 기본 주소 |
| data.상세주소 | string | 상세 주소 |
| data.가입일 | string | 가입일 (YYYY-MM-DD) |
| data.최근로그인 | string | 최근 로그인일 (YYYY-MM-DD) |
| data.회원등급 | string | 회원 등급 (BRONZE, SILVER, GOLD 등) |
| data.적립포인트 | number | 적립 포인트 |
| data.총주문수 | number | 총 주문 수 |
| data.총주문금액(원) | number | 총 주문 금액 |
| data.상태 | string | 회원 상태 (활성, 비활성 등) |
| data.마케팅동의 | string | 마케팅 동의 여부 (Y/N) |

---

## 3️⃣ 회원 주문 및 송장번호 조회

### 요청

```
GET /api/orders/member/:memberId/tracking
```

### 파라미터

| 파라미터 | 타입 | 위치 | 필수 | 설명 |
|---------|------|------|------|------|
| memberId | string | path | ✅ | 회원ID (예: USR-0001) |

### 요청 예시

```bash
curl http://YOUR_SERVER_IP:3000/api/orders/member/USR-0001/tracking
```

### 응답 예시

**성공 (200 OK)**

```json
{
  "success": true,
  "data": [
    {
      "주문ID": "ORD-00001",
      "송장번호": "811518691982",
      "택배사": "한진택배",
      "배송상태": "배송완료",
      "배송추적URL": "https://tracker.example.com/한진택배/811518691982"
    },
    {
      "주문ID": "ORD-00002",
      "송장번호": "787001876766",
      "택배사": "로젠택배",
      "배송상태": "배송완료",
      "배송추적URL": "https://tracker.example.com/로젠택배/787001876766"
    }
  ]
}
```

**실패 (404 Not Found)**

```json
{
  "success": false,
  "message": "주문 정보를 찾을 수 없습니다."
}
```

### 응답 필드 설명

| 필드 | 타입 | 설명 |
|------|------|------|
| success | boolean | 요청 성공 여부 |
| data | array | 주문 및 송장번호 정보 배열 |
| data[].주문ID | string | 주문 고유 ID |
| data[].송장번호 | string | 송장번호 |
| data[].택배사 | string | 택배사명 |
| data[].배송상태 | string | 배송 상태 |
| data[].배송추적URL | string | 배송 추적 URL |

### 비고

- 회원ID로 해당 회원의 모든 주문을 조회합니다
- 각 주문에 대한 송장번호와 배송정보를 함께 반환합니다
- 주문일시 기준 내림차순으로 정렬됩니다

---

## 4️⃣ 송장번호로 배송정보 조회

### 요청

```
GET /api/shipments/tracking/:trackingNumber
```

### 파라미터

| 파라미터 | 타입 | 위치 | 필수 | 설명 |
|---------|------|------|------|------|
| trackingNumber | string | path | ✅ | 송장번호 (예: 811518691982) |

### 요청 예시

```bash
curl http://YOUR_SERVER_IP:3000/api/shipments/tracking/811518691982
```

### 응답 예시

**성공 (200 OK)**

```json
{
  "success": true,
  "data": {
    "배송ID": "SHP-00001",
    "주문ID": "ORD-00001",
    "택배사": "한진택배",
    "송장번호": "811518691982",
    "배송상태": "배송완료",
    "발송일": "2025-02-07",
    "예상도착일": "2025-02-10",
    "실제도착일": "2025-02-11",
    "수령인": "권예준",
    "수령인연락처": "010-7806-8484",
    "배송주소": "대전광역시 유성구 101동 1502호",
    "배송메모": "부재시 문앞에 놓아주세요",
    "배송추적URL": "https://tracker.example.com/한진택배/811518691982"
  }
}
```

**실패 (404 Not Found)**

```json
{
  "success": false,
  "message": "배송정보를 찾을 수 없습니다."
}
```

### 응답 필드 설명

| 필드 | 타입 | 설명 |
|------|------|------|
| success | boolean | 요청 성공 여부 |
| data | object | 배송정보 객체 |
| data.배송ID | string | 배송 고유 ID |
| data.주문ID | string | 주문 ID |
| data.택배사 | string | 택배사명 |
| data.송장번호 | string | 송장번호 |
| data.배송상태 | string | 배송 상태 (집하완료, 배송출발, 배송완료 등) |
| data.발송일 | string | 발송일 (YYYY-MM-DD) |
| data.예상도착일 | string | 예상 도착일 (YYYY-MM-DD) |
| data.실제도착일 | string | 실제 도착일 (YYYY-MM-DD, null 가능) |
| data.수령인 | string | 수령인 이름 |
| data.수령인연락처 | string | 수령인 연락처 |
| data.배송주소 | string | 배송 주소 |
| data.배송메모 | string | 배송 메모 |
| data.배송추적URL | string | 배송 추적 URL |

---

## 📊 공통 응답 형식

### 성공 응답

모든 성공적인 요청은 다음 형식을 따릅니다:

```json
{
  "success": true,
  "data": { ... }
}
```

### 에러 응답

모든 에러 응답은 다음 형식을 따릅니다:

```json
{
  "success": false,
  "message": "에러 메시지"
}
```

## 🔢 HTTP 상태 코드

| 상태 코드 | 설명 |
|----------|------|
| 200 | 요청 성공 |
| 404 | 리소스를 찾을 수 없음 |
| 500 | 서버 내부 오류 |

## 📝 사용 예시

### JavaScript (Fetch API)

```javascript
// 상품 정보 조회
fetch('http://YOUR_SERVER_IP:3000/api/products/PRD-0001')
  .then(response => response.json())
  .then(data => {
    if (data.success) {
      console.log('상품 정보:', data.data);
    } else {
      console.error('에러:', data.message);
    }
  })
  .catch(error => console.error('네트워크 오류:', error));
```

### Python (requests)

```python
import requests

# 상품 정보 조회
response = requests.get('http://YOUR_SERVER_IP:3000/api/products/PRD-0001')
data = response.json()

if data['success']:
    print('상품 정보:', data['data'])
else:
    print('에러:', data['message'])
```

### cURL

```bash
# 상품 정보 조회
curl http://YOUR_SERVER_IP:3000/api/products/PRD-0001

# 회원 정보 조회
curl http://YOUR_SERVER_IP:3000/api/members/USR-0001

# 회원 주문 및 송장번호 조회
curl http://YOUR_SERVER_IP:3000/api/orders/member/USR-0001/tracking

# 송장번호로 배송정보 조회
curl http://YOUR_SERVER_IP:3000/api/shipments/tracking/811518691982
```

## 🔗 관련 링크

- **Swagger 문서**: `http://YOUR_SERVER_IP:3000/api-docs`
- **Health Check**: `http://YOUR_SERVER_IP:3000/health`

## ⚠️ 주의사항

1. 모든 API는 GET 메서드만 지원합니다
2. 인증/인가가 필요한 경우 별도로 구현해야 합니다
3. Base URL의 `YOUR_SERVER_IP`는 실제 서버 IP 주소로 변경해야 합니다
4. 프로덕션 환경에서는 HTTPS 사용을 권장합니다

---

**문서 버전**: 1.0.0  
**최종 업데이트**: 2025년
